allprojects {
    group = 'org.pkaq.gradle.multi'
    version = "0.1.0"
}

subprojects {
   apply plugin: 'java'
}


project(':main'){	

	apply plugin: 'war'

	dependencies {
    	compile  project(':base')
	}

}
